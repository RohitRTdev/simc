#include "preprocessor/preprocess.h"
#include "debug-api.h"


//void preprocess::handle_directive() {
    //CRITICAL_ASSERT(handle_directives, "handle_directive() called on wrong context");
    //
    ////Read the next keyword to know the particular define statement
    //while(is_white_space()) 
        //buffer_index++;
    //
    //std::string directive;
    //while(isalnum(contents[buffer_index])) {
        //directive += contents[buffer_index++];
    //}
//
    //if(directive == "define") {
        //handle_define();
    //} 
    //else {
        //sim_log_error("Unknown preprocessor directive used: {}", directive);
    //}
//}

